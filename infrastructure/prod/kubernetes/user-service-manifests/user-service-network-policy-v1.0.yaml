apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-service-v1.0
  namespace: prod
spec:
  podSelector:
    matchLabels:
      app: user-service
      version: v1.0
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: prod
      ports:
        - protocol: TCP
          port: 8080
provider:
  name: cloudflare
sources:
  - service
  - ingress
  - istio-gateway
  - istio-virtualservice
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-key
        key: apiKey
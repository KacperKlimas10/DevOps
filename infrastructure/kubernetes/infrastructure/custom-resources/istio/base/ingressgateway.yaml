apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: application-gateway
  namespace: istio-ingress
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http2
        protocol: HTTP2
      hosts:
        - devops.kacperklimas.com
        - argocd.kacperklimas.com
        - grafana.kacperklimas.com
      tls:
        httpsRedirect: true # Redirect from HTTP to HTTPS
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - devops.kacperklimas.com   # This should match a DNS name in the Certificate
      tls:
        mode: SIMPLE
        credentialName: app-ingress-gateway-cert  # This should match the Certificate secretName
        minProtocolVersion: TLSV1_2 # The best option is to use TLSv1.2 as the minimum protocol version, but the proxy should preferably use TLSv1.3
        maxProtocolVersion: TLSV1_3
        cipherSuites:
          - ECDHE-ECDSA-AES128-GCM-SHA256
          - ECDHE-RSA-AES128-GCM-SHA256
          - ECDHE-ECDSA-AES128-SHA
          - AES128-GCM-SHA256
          - AES128-SHA
          - ECDHE-ECDSA-AES256-GCM-SHA384
          - ECDHE-RSA-AES256-GCM-SHA384
          - ECDHE-ECDSA-AES256-SHA
          - AES256-GCM-SHA384
          - AES256-SHA